# Reviewed native contracts

The parity pipeline consumes JSON recipes here after explicit independent ABI
and semantic review. `world.json` is the first reviewed family; bulk binary
discoveries are not automatically approved functions. See
`docs/PARITY_AUTOMATION.md` for the format.
Do not place upstream source, reconstructed headers or address databases here.
